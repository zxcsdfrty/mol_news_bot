# 【替代方案】部署在機關內部 Linux 主機（不使用 GitHub Actions）
# crontab -e 加入下行；flock 防止重疊執行
*/30 * * * * cd /opt/mol-news-bot && /usr/bin/flock -n /tmp/mol-news-bot.lock .venv/bin/python -m news_bot.main >> logs/news_bot.log 2>&1
